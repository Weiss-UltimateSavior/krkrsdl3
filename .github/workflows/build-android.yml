name: Build Android

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths-ignore:
      - '**.md'
      - 'Res/**'
      - 'emscripten/**'
      - 'ohos/**'
  pull_request:
    branches: [main]

jobs:
  build-native:
    name: Build Native (${{ matrix.abi }})
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        include:
          - abi: arm64-v8a
            triplet: arm64-android
          - abi: armeabi-v7a
            triplet: arm-android

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Android NDK
        uses: nttld/setup-ndk@v1
        id: setup-ndk
        with:
          ndk-version: r28

      - name: Setup vcpkg
        uses: lukka/run-vcpkg@v11
        with:
          vcpkgDirectory: ${{ github.workspace }}/vcpkg-root

      - name: Install Ninja
        run: sudo apt-get install -y ninja-build

      - name: Configure & Build
        run: |
          cmake -B out/android/${{ matrix.abi }} \
            -DCMAKE_TOOLCHAIN_FILE=${{ github.workspace }}/vcpkg-root/scripts/buildsystems/vcpkg.cmake \
            -DVCPKG_CHAINLOAD_TOOLCHAIN_FILE=${{ steps.setup-ndk.outputs.ndk-path }}/build/cmake/android.toolchain.cmake \
            -DVCPKG_TARGET_TRIPLET=${{ matrix.triplet }} \
            -DANDROID=true \
            -DANDROID_ABI=${{ matrix.abi }} \
            -DANDROID_PLATFORM=android-28 \
            -DCMAKE_SYSTEM_NAME=Android \
            -DCMAKE_SYSTEM_VERSION=28 \
            -DCMAKE_BUILD_TYPE=Release \
            -G Ninja
          cmake --build out/android/${{ matrix.abi }} --config Release

      - name: Collect Libraries
        run: |
          mkdir -p out/libs/${{ matrix.abi }}
          cp out/android/${{ matrix.abi }}/libkrkrsdl3.so out/libs/${{ matrix.abi }}/
          find ${{ github.workspace }}/vcpkg-root/installed/${{ matrix.triplet }} -name "*.so" \
            -exec cp {} out/libs/${{ matrix.abi }}/ \; 2>/dev/null || true
          echo "Collected libraries:"
          ls -la out/libs/${{ matrix.abi }}/

      - name: Upload Native Libraries
        uses: actions/upload-artifact@v4
        with:
          name: native-libs-${{ matrix.abi }}
          path: out/libs/${{ matrix.abi }}/

  build-apk:
    name: Build APK
    needs: build-native
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Download arm64-v8a libraries
        uses: actions/download-artifact@v4
        with:
          name: native-libs-arm64-v8a
          path: native-libs/arm64-v8a

      - name: Download armeabi-v7a libraries
        uses: actions/download-artifact@v4
        with:
          name: native-libs-armeabi-v7a
          path: native-libs/armeabi-v7a

      - name: Place libraries for APK packaging
        run: |
          mkdir -p android/app/libs/arm64-v8a android/app/libs/armeabi-v7a
          cp native-libs/arm64-v8a/*.so android/app/libs/arm64-v8a/
          cp native-libs/armeabi-v7a/*.so android/app/libs/armeabi-v7a/
          echo "Libraries for APK:"
          find android/app/libs -name "*.so" -exec ls -la {} \;

      - name: Setup JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: Setup Android SDK
        uses: android-actions/setup-android@v3

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v4

      - name: Build Release APK
        working-directory: android
        run: ./gradlew assembleRelease -PskipNativeBuild

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: krkrsdl3-release-apk
          path: out/android/app/outputs/apk/release/*.apk

  merge-libs:
    name: Merge Native Libs
    needs: build-native
    runs-on: ubuntu-latest
    steps:
      - name: Merge
        uses: actions/upload-artifact/merge@v4
        with:
          name: krkr-android-libs
          pattern: native-libs-*
          delete-merged: true
